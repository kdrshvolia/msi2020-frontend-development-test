import React from 'react';import Radio from '../Radio/Radio';import Button from '../Button/Button';const SearchJokeForm = ({ setType, getJoke }) => {  return (    <div>      <Radio name="search-type" value="Random" handleChange={setType} />      <Radio name="search-type" value="From categories" handleChange={setType} />      <Radio name="search-type" value="Search" handleChange={setType} />      <Button text="Get a joke" onClick={getJoke} />    </div>  );};export default SearchJokeForm;