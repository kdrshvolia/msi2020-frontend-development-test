import { createGlobalStyle } from 'styled-components';

const GlobalStyle = createGlobalStyle`
  body, html {
   font-family: Roboto;
    font-style: normal;
    font-weight: normal;
    color: #333333;
    height: 100%;
  }
`;

export { GlobalStyle };
