import { createGlobalStyle } from 'styled-components';

const GlobalStyle = createGlobalStyle`
  body {
   font-family: Roboto;
    font-style: normal;
  font-weight: normal;
  color: #333333;
  }
`;

export { GlobalStyle };
